#include<stdio.h>
#include<conio.h>
main()
{
	int a, b, c;
	clrscr();

	printf("Enter first number: ");
	scanf("%d",&a);
	printf("Enter second number: ");
	scanf("%d",&b);
	printf("Enter third number: ");
	scanf("%d",&c);

	if(a<b)
	{
		printf("%d is minimum number",a);
	}
	else
	{
		if(b<c)
		{
			printf("%d if minimum number",b);
		}
		else
		{
			printf("%d is minimum number",c);
		}
	}

	getch();
}